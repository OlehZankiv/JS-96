/*
 * Інтерфейс classList
 * - add
 * - remove
 * - toggle
 * - replace
 * - contains
 */

const currentPageUrl = '/contact';

const linkEl = document.querySelector(
  `.site-nav__link[href="${currentPageUrl}"]`,
);


// linkEl.classList.toggle("site-nav__link");
// linkEl.classList.replace("site-nav__link", "hello");
// linkEl.classList.replace("site-nav__link", "hello");
// if (linkEl.classList.contains("hello")) {
//   console.log("Contain");
// }
